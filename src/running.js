// eslint-disable-next-line
console.log('@文件Running加载');

// eslint-disable-next-line
export default function () {
  const running = 'running';
  // eslint-disable-next-line
  console.log("@RUNNING！", running);
}
